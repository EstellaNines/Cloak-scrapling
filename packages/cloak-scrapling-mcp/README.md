# cloak-scrapling-mcp

`cloak-scrapling-mcp` is a uvx-friendly MCP wrapper package for
`cloak-scrapling`.

It exposes the same MCP server command:

```bash
uvx cloak-scrapling-mcp
```

For local source development:

```bash
uvx --from packages/cloak-scrapling-mcp cloak-scrapling-mcp --help
```

